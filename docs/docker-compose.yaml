services:
  docs:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm start -- --host 0.0.0.0"
    stdin_open: true
    tty: true
